# Tipels - Runtime Dependencies
# Python 3.10+

# HINWEIS: PyGObject und dbus-python NICHT über pip installieren!
# Diese müssen über System-Pakete installiert werden:
#   sudo apt install python3-gi python3-dbus

# CLI Framework
click>=8.1.0

# HTTP Requests (Treiber-Download)
requests>=2.28.0

# System-Dependencies (MUSS über apt installiert werden):
# - python3-gi             # PyGObject (GTK-Bindings)
# - python3-gi-cairo       # Cairo-Support
# - gir1.2-gtk-3.0         # GTK 3
# - python3-dbus           # D-Bus Python-Bindings
# - cups                   # CUPS Drucksystem
# - sane                   # Scanner-Support
# - sane-utils             # Scanner-Tools
# - avahi-daemon           # Netzwerk-Erkennung
# - avahi-utils            # Avahi-Tools
